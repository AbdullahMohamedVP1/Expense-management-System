# Root of your backend API (no trailing slash, no /expense or /income suffix).
# Must match the backend's route prefix (see backend/server.js: app.use("/api/v1/...")).
# Each API module appends its own path, e.g. VITE_API_URL + /expense/get
VITE_API_URL=http://localhost:5173/api/v1
